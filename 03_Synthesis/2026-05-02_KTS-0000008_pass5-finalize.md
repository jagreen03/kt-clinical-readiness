# KTS-0000008 Pass 5: README, Proxy Config, & Saga Finalization

## Decisions & Architecture Notes

* **proxy.conf.json & Same-Origin Model**: Configured the Angular dev server to tunnel `/api`, `/auth`, `/oauth2`, and `/login/oauth2` to the BFF at `localhost:8080`. This provides a true same-origin local development experience, natively supporting Angular's built-in XSRF handling without cross-origin cookie drops.
* **README Structure & Doctrine Assumption**: As I am unable to read the disk to confirm the presence of a prior KTS-0000005 `README.md`, I am proceeding under the authorized fallback directive to create a fresh, senior-grade README documenting the integration architecture, build commands, and known limitations.
* **Junior Developer Perspective (The CORS/CSRF Local Dev Trap)**: A common junior stumbling block is fighting CORS and CSRF token drops during local development by writing complex HTTP interceptors or weakening backend security policies. The lesson here is environmental parity: using `proxy.conf.json` aligns the dev environment with the production same-origin reality, solving the security friction at the infrastructure level rather than the application level.

## Saga Close-out Summary

**What Works (Completed):**
* Angular 19+ zoneless SPA wired with `provideHttpClient` (withFetch).
* `AuthService` with robust signal-based tri-state (`undefined` | `null` | `User`).
* Full Google OAuth login/logout lifecycle via the Spring Boot BFF.
* Centralized `errorInterceptor` handling 401s cleanly without state race conditions.
* End-to-end proxy demonstrator (`ProxyDemoComponent`) successfully fetching downstream data.

**Known Limitations & Carry-Forward List:**
* `provideClientHydration` removal (technical debt; currently inactive with no SSR).
* Custom `csrfInterceptor` removal (pending finalized prod topology; currently kept as a defensive duplicate to the proxy conf).
* Component `.spec.ts` testing files missing (future polish pass).
* `HomeComponent` and `DashboardComponent` remain inline placeholders in `app.routes.ts` pending real feature implementation.
* Lessons-learned doctrine consolidation (Junior dev perspectives from Passes 3, 4, and 5 should be folded into `05_Wiki/Triad/lessons-learned.md`).

**Next Saga Candidate (KTS-0000009):**
* Either begin feature implementation on the inline placeholder components, or execute a targeted technical debt saga to clear the accumulated carry-forward lists from KTS-0000007 and KTS-0000008.